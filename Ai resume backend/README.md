# AI Resume Tracker API

## Setup

1. Copy `.env.example` to `.env` and set `MONGO_URI` and `GEMINI_API_KEY`.
2. Run `npm install`.
3. Run `npm run dev`.

The API starts on `http://localhost:5000` by default. Use `GET /api/health` to confirm it is running.
